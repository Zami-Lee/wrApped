import 'package:flutter/material.dart';

class PhotoSharingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Photo Sharing Page',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
